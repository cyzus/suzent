# Discord response rules

- Use Discord Markdown; avoid wide tables.
- The driver does not auto-chunk the usual 2000-character limit. Split long responses before
  delivery.
- Outbound files are supported; native option buttons are not. Use numbered choices instead.
