# Slack response rules

- Use Slack `mrkdwn`: `*bold*`, Slack-compatible links, and bold lines instead of Markdown
  headings.
- Outbound files are supported, but incoming file metadata may not include downloadable
  content. Native option buttons are unsupported; use numbered choices.
