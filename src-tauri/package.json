{
  "name": "suzent-desktop",
  "version": "0.2.7",
  "description": "SUZENT - Your Personal Agent (Desktop Application)",
  "private": true,
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build --config tauri.conf.prod.json",
    "build:frontend": "cd ../frontend && npm install && npm run build",
    "bundle:python": "uv run python ../scripts/bundle_python.py",
    "build:full": "npm run bundle:python && npm run build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0-rc.18"
  }
}
